namespace Platformer.AnimationSystem
{
    public enum PlayerAnimation
    {
        IDLE,
        RUNNING
    }
    
    public enum PlayerTriggerAnimation
    {
        JUMP,
        SLIDE,
        ATTACK,
        TAKE_DAMAGE,
        DEATH,
    }
}