namespace Platformer.Player
{
    public enum PlayerAnimation
    {
        IDLE,
        RUNNING
    }
    
    public enum PlayerTriggerAnimation
    {
        JUMP,
        SLIDE,
        ATTACK,
        TAKE_DAMAGE,
        DEATH,
    }
}